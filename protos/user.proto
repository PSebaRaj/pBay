syntax = "proto3";
package user;
option go_package = "../user";

message UserData {
	string name = 1;
	string email = 2;
	bool activeStatus = 3;
}

message UserEmail {
	string email = 1;
}

service UserService {
	rpc SendUserData(UserEmail) returns (UserData) {}
}
